# Interactive notebooks

Jupyter notebooks for hands-on lessons. Run `jupyter notebook` from the repo root.
Each notebook pairs with a module README (e.g. a metrics notebook for `05-metrics-and-evaluation`).
